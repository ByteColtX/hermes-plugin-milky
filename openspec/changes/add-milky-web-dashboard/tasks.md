# Tasks

## 1. 宿主接入与配置读取

- [ ] 1.1 在隔离的真实 Hermes 用户插件安装中验证 Dashboard 发现、插件 API 挂载、宿主认证二进制请求、profile 作用域和路由关闭能力；以记录 core/SDK 版本的能力矩阵为验收，缺失项标 unsupported，不修改 core。
- [ ] 1.2 建立宿主管理入口和预构建资源交付，保持 QQ 平台唯一注册与导入无副作用；通过缺配置仍可访问页面、仅 Gateway 启动不加载 Web 依赖的测试，以及安装目录资源检查验收。
- [ ] 1.3 实现普通配置 settings → legacy config → 当前 profile 环境 → 默认值的统一解析；通过纯环境、纯 YAML、混合、缺失、非法高优先级、false/0/空列表及 Will 整体选源测试验收。
- [ ] 1.4 对齐 manifest 配置类型、secret 声明和平台发现条件，保留现有 Action 与贴纸工具；通过没有旧地址环境变量但有效 YAML 地址可启动、所有运行入口保持同一快照的测试验收。

## 2. 配置管理与作用域

- [ ] 2.1 提供显式值、有效值、来源、可写性及配置版本读取，凭证仅返回存在状态；通过不同 profile、托管覆盖、损坏配置和敏感字段输出测试验收。
- [ ] 2.2 实现普通字段完整候选校验、仅提交变更键、保存前版本检查和逐项保存后核验；通过未知字段、值域错误、旧表单、托管拒绝、部分失败与并发变化测试验收，并明确 core 不提供跨入口条件事务的限制。
- [ ] 2.3 接入 core 凭证保持、替换和清除，普通设置与凭证独立保存；通过首次分步配置、拒写后核验和空输入保持测试验收，确认响应、任务和日志不含 token。
- [ ] 2.4 为每个管理请求和后台任务绑定宿主确认的 profile，并检查插件启用状态；通过两个 profile 并发、伪造范围、停用和切换后旧响应测试验收，确认不会跨范围补值或写库。

## 3. 共享图库与只读查询

- [ ] 3.1 增加只读图库分页、筛选、描述检索和字段来源展示；通过默认 20/上限 100、稳定排序、空库不创建、损坏库不重建及统计不变测试验收。
- [ ] 3.2 提供按可见 ID 获取的认证预览，复核路径、类型、大小与完整性，并限制缓存；通过跨 profile ID、已删除、缺失、损坏和路径伪造测试验收，确认媒体不进入日志或任务摘要。
- [ ] 3.3 建立各维护入口共享的跨进程提交协调和条目版本，视觉调用位于短提交保护之外；通过独立进程同时导入相同内容、编辑与重新分析竞争测试验收，确保唯一条目及人工覆盖不丢失。
- [ ] 3.4 协调删除/清理与预览、发送的一次性本地读取，保持原发送统计及网络边界；通过并发读取和回收测试验收，确认保护不跨 Milky 网络请求且不会重复发送。

## 4. 浏览器上传与批次导入

- [ ] 4.1 增加独立于命令 inbox 的受控上传暂存和不透明批次归属；通过不可信文件名、URL/压缩包拒绝、实际 bytes 限制、50 文件/100 MiB 批次边界及断线回收测试验收。
- [ ] 4.2 将 Web 明确候选集合接入现有校验、去重、辅助视觉和入库语义；通过合成 PNG/JPEG/GIF/WebP、10 MiB 单图边界、重复、非贴纸、视觉异常和移动失败测试验收，确认不夹带 inbox 或其他批次文件。
- [ ] 4.3 实现失败输入显式重试/丢弃、每 profile 500 MiB 暂存配额和无活动引用 7 天回收；通过配额竞争、活动引用保护与时间推进测试验收，确认不触碰命令 inbox 或 junk。

## 5. 批量维护与修复

- [ ] 5.1 提供单项和最多 50 个明确去重 ID 的批量部分编辑、覆盖清除和重新分析；通过字段原子校验、版本冲突、人工覆盖保留、统计不变和逐项部分成功测试验收。
- [ ] 5.2 实现按 ID 与版本执行的批量删除，沿用库文件引用检查和提交顺序；通过未知 ID、多引用异常、重复请求和删除中断测试验收，确认不扩大目标或误删文件。
- [ ] 5.3 提供清理预览、绑定范围的执行计划及索引修复；通过预览后新引用、过期计划、新增文件、缺失文件、junk 排除及索引事务失败测试验收。

## 6. 任务执行与关闭

- [ ] 6.1 为图库变更提供持久任务、逐项结果和统一状态，落实每 profile 一个运行批次、最多 10 个排队及既有视觉并发上限；通过队列满、部分成功、同步视觉不阻塞请求和状态转换测试验收。
- [ ] 6.2 实现有期限请求标识、相同提交返回原任务、不同内容冲突和历史淘汰后的 expired；通过双击、断线重提、进程重启、7 天/1000 条终态保留测试验收，确认去重记录有界且活动任务不淘汰。
- [ ] 6.3 实现取消、停用、宿主关闭及启动后 interrupted 恢复；通过在途视觉迟到结果、派发/提交前停用和提交前后中断测试验收，确认不自动重放、不晚写已关闭存储、不误报成功。
- [ ] 6.4 保持命令与 Dashboard 资源独立所有权及普通消息隔离；通过 adapter 断开不关闭 Web 任务、Dashboard 关闭不改变 QQ 状态和日志脱敏测试验收。

## 7. Dashboard 页面流程

- [ ] 7.1 使用宿主组件、主题与认证客户端构建页面和明确的 profile 选择；通过宿主加载、窄屏、键盘操作、加载/错误状态和切换范围检查验收。
- [ ] 7.2 实现全部正式配置、Will 分组/JSON 同步、字段错误和独立凭证操作；通过首次配置、未展开字段保留、白名单空值提示、部分保存和待重启/未知运行态浏览器流程验收。
- [ ] 7.3 实现图库筛选分页、认证预览、上传结果、单项及批量编辑/删除/重新分析；通过明确选择范围、删除确认、冲突逐项展示和离页释放预览资源的浏览器流程验收。
- [ ] 7.4 实现清理计划确认、索引修复、任务进度/取消/恢复查看及过期提示；通过页面断线重连不重提任务、切换 profile 不混入旧任务结果的浏览器流程验收。

## 8. 集成证据与交付

- [ ] 8.1 使用合成图片及独立 profile，在真实 Hermes Dashboard 验证完整配置、上传、预览、编辑、批量维护及重启流程；形成带版本和执行结果的 evidence，明确 fixture/fake host 与真实宿主证据，视觉服务不可用时标 blocked。
- [ ] 8.2 验证目录插件前端可重复构建、预构建资源齐全及宿主代理前缀下加载；以锁定依赖、构建检查和真实用户插件安装检查验收，不以 Python wheel 构建替代目录发布验证。
- [ ] 8.3 更新 README、ARCHITECTURE 的配置来源、Dashboard 安装、保存生效、暂存配额、任务状态、回滚步骤和维护入口说明；逐项核对实现与四份 delta，并准备主维护规范 Purpose 同步，未验证能力明确保留状态。
- [ ] 8.4 执行前后端相关测试与构建，并运行 uv run pytest -q、uv run ruff check .、uv run ruff format --check .、uv build、git diff --check、openspec validate --changes --strict；以命令结果和 skip/外部阻塞记录验收，不把未完成的真实集成标为通过。
