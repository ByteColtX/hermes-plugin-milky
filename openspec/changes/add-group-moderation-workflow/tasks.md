# Tasks

## 1. 配置与兼容边界

- [ ] 1.1 增加群管普通配置、三档模式、群规与本地规则、动作权限、上下文及资源限额校验；以配置测试验证缺省关闭、启用默认 auto、非法群与无效策略不生效。
- [ ] 1.2 接入宿主独立辅助模型配置与能力探测，不在注册阶段联网；以 fake host 测试确认发现行为、缺失能力 unsupported 和主会话不被启动。
- [ ] 1.3 更新 manifest 与 README 的配置、按群启用及三个既有工具兼容变更说明；核对字段和默认值与配置测试一致，检查 add-hot-chat-allowlist 是否已落地并保留其公共有效范围语义。

## 2. 持久案件与短期证据

- [ ] 2.1 增加 profile/Bot 隔离的案件、方案版本、单次批准和动作状态存储；以独立数据库连接测试覆盖重复事件、并发批准/拒绝、旧版本冲突及重启恢复。
- [ ] 2.2 增加独立短期证据存储、受限访问、24 小时清理和 64 MiB 配额；以合成正文测试身份隔离、URL/已知凭证排除、缺失标记与满额拒绝，不把正文写入执行记录。
- [ ] 2.3 增加案件容量、终态保留、24 小时消息接纳与持久去重凭证容量保护；以可控时间测试历史淘汰后重放、时间不明、过旧事件和配额满载均不能再次处罚。
- [ ] 2.4 在 ARCHITECTURE.md 记录案件、证据及工作者所有权和状态恢复边界；核对所有状态均有对应测试且 unknown 不被恢复成可重试。

## 3. 本地筛查和独立上下文

- [ ] 3.1 在已校验消息的去重与同群有序接纳后接入群管资格和本地筛查；以 pipeline 测试验证未触发聊天仍审核、mute 不阻断合资格审核、Self/范围外/temp/系统事件不审核。
- [ ] 3.2 实现字面词、精确例外及受限格式/刷屏规则，保留真实消息引用；以合成规则用例及有界输入基准验证正常文本零模型调用、相同正文不同序号保留、超限匹配不阻塞聊天。
- [ ] 3.3 建立独立近期上下文和命中快照，包含非命中消息并标注缺失/截断；以顺序测试覆盖 N、时间窗、字符上限、聊天 drain、跨群隔离和当前消息不重复。
- [ ] 3.4 更新 README 的筛查配置示例与覆盖限制；以规则测试核对示例，不把词表命中描述为已确认违规。

## 4. 辅助模型复核与三档路由

- [ ] 4.1 接入独立结构化辅助模型复核，限制输入来源并校验规则、证据、目标、动作及参数；用 fake host 验证引用误报被关闭、正文提示不改变权限、伪造目标/畸形输出不执行。
- [ ] 4.2 实现 auto/smart/manual 决策路由和受限动作政策；以决策矩阵测试验证超时、模型不可用、非法方案、关闭踢人、时长越界以及手动接管的准确状态。
- [ ] 4.3 实现有界审核队列、模型超时与覆盖缺口计数；以阻塞模型替身测试并发上限、满载继续聊天、断开取消和重复投递只审核一次。
- [ ] 4.4 更新 ARCHITECTURE.md 与 README，说明独立辅助模型、三档差异和无会话授权；核对模型输入不继承主会话历史、记忆及工具执行循环。

## 5. 统一群管执行控制

- [ ] 5.1 利用初始群成员同步建立管理身份状态，并在每步执行前刷新自身及目标身份；用协议 fixture 覆盖 admin/owner/member、未知值、错误群/成员、权限撤销和受保护目标。
- [ ] 5.2 实现真实案件绑定、有效白名单、策略/方案版本、有效期及动作政策检查；以执行测试验证过期、改参数、重载禁用和越权 Web 批准均不发出处置请求。
- [ ] 5.3 实现排他领取、发送前持久标记和逐动作单次提交；以多连接及进程崩溃故障注入验证重启、重复批准、Tool/后台竞争最多一次发送，在途 unknown 永不自动重发。
- [ ] 5.4 接入三个固定 Action 的后台处置及组合结果；以 Milky fixture 验证成功 envelope、HTTP 200 协议失败、畸形响应、传输中断和部分成功准确区分。
- [ ] 5.5 将三个既有 Tool 入口纳入同一控制，可信关联缺失时 blocked、等待审批时 pending_review 立即返回；用工具测试确认通用 approval 不绕过、合法远端响应仍逐字原样交付。
- [ ] 5.6 更新 README 的旧工具迁移行为及 ARCHITECTURE.md 执行边界；通过静态调用点检查确认不存在三个动作的未保护路径，不宣称其余工具已完成授权改造。

## 6. Web 审阅和审批

- [ ] 6.1 在现有认证及 profile 范围内增加群管配置、运行状态和规则试验；以 API 测试验证配置保存/生效区分、只测试规则不调用模型、跨 profile 与停用请求拒绝。
- [ ] 6.2 增加案件分页筛选、纯文本证据、方案参数、版本和逐步结果视图；以页面测试验证 no-store、HTML 不执行、无媒体自动加载、切换 profile 不串数据、证据过期提示。
- [ ] 6.3 增加批准、拒绝、未决接管和过期重评入口，绑定宿主确认的操作者及方案版本；以请求测试覆盖双击、旧表单、服务端角色/策略复查及无证据不可重新处罚。
- [ ] 6.4 将 Web 与 Gateway 通过持久案件衔接，Web 只保存决策；用独立进程测试验证 Gateway 离线可审阅、恢复后重查、浏览器关闭不丢案件且 Web 不建立处置连接。
- [ ] 6.5 更新页面帮助和 README，说明案件保留、方案 10 分钟与证据 24 小时的区别及重新审核方式；核对 UI 文案不宣称待审批已执行或未知结果成功。

## 7. 生命周期与综合验收

- [ ] 7.1 接入连接停止、重载和卸载的取消等待与资源关闭；以生命周期测试验证注册零网络、重复停止、多实例所有权和持久数据保留，并同步生命周期文档。
- [ ] 7.2 跑合成端到端矩阵：未触发聊天、本地命中、三档路由、Web 批准、权限撤销、未知结果和重启；用发送记录断言群/私聊/home channel 从未出现授权申请。
- [ ] 7.3 在真实 Hermes 上验证辅助模型、宿主身份、配置与跨进程 Web 流程；将真实证据与 fake host 分开记录，能力缺失或 skip 明确标注，不能写为联调通过。
- [ ] 7.4 仅在获得明确授权和指定测试群/成员后验证真实 Milky 撤回、禁言、踢人及失败结果；记录 operationId、低敏结果及未确认边界，不提交正文或 live 响应；缺少授权时登记外部验证阻塞。
- [ ] 7.5 运行 uv run pytest -q、uv run ruff check .、uv run ruff format --check .、uv build、git diff --check 和 openspec validate --changes --strict；整理 evidence ledger 并核对实现、manifest、文档和规范的默认值、错误及状态一致。
