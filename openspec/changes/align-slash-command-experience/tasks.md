# Tasks

## 1. 统一静态帮助与参数反馈

- [ ] 1.1 实现顶层和贴纸静态帮助、空 sticker 帮助及共用文本约定，同步精简 allowlist 帮助，保留标题、缺省语义和删除别名，在 Commands 末尾用一行 e.g. 展示 add/del 共用的合成目标及通配符，明确标注“别名 remove”，取消独立 Targets/Examples 章节，顶层采用 /milky [command] [args...] 并把无参数行为移出 Commands；用文本契约和 fake 依赖断言所有帮助在零/单/多活动实例及图库缺失下均不访问网络、配置、目录或数据库。
- [ ] 1.2 覆盖顶层、sticker 全部动词和 allowlist 的参数错误，提供就近 Usage 与 Help，已知 allowlist 动词只显示自身语法、list/help 不显示目标；参数化验证未知命令、help/status 多余参数、非法 ID/目标/选项均在业务操作前失败，不回显合成敏感输入、不回退其他分支。
- [ ] 1.3 增加 sticker remove 与 del 等价语法，保留大小写规则和其余操作参数；用相同初始状态验证别名的合法/缺参/非法 ID/未知条目结果一致且单次最多删除一次，并同步 README 的命令与别名示例。
- [ ] 1.4 更新注册描述与 README 帮助入口，说明无参数 /milky 继续查询实现信息，Commands 列出 status，顶层和 allowlist 不重复列 Examples，贴纸只保留两个复杂用法示例；逐条核对帮助中的动词、选项、枚举、默认值和示例与解析行为一致，运行 slash 与 allowlist 聚焦测试通过。

## 2. 统一查询、维护与失败回执

- [ ] 2.1 将实现信息标题与全部安全失败分类映射为中文回执；更新成功及错误 fixture 断言，验证字段完整、未知扩展字段和异常不泄漏，零/多 client 不发请求，正常查询仅调用原有只读 Action 一次。
- [ ] 2.2 将贴纸列表、空库和 edit/reanalyze/del 的单项结果渲染为中文，保留 ID、受限元数据、字段来源、情绪枚举和使用统计；测试稳定顺序、本次返回数、空使用时间、成功/不存在/缺失/非贴纸/竞争/存储失败及 unsupported，确认文案与真实状态匹配。
- [ ] 2.3 为 add/cleanup/reindex 实现分类计数、零处理与混合结果摘要，为两种 dry-run 提供明确预览及逐候选必要数据；通过合成混合批次验证不把 ok 等同全部成功、不合计不同单位、不把待操作写成已执行，且文件/条目/统计副作用保持原契约。
- [ ] 2.4 迁移贴纸维护、搜索、发送测试中对 slash JSON 的数据准备依赖，保留结构化业务断言并新增独立命令文本断言；运行贴纸维护/管理/搜索/发送/回退相关测试，验证 Web 与 Tool 仍使用原字段和固定分类。
- [ ] 2.5 在 README 中提供最终帮助、格式错误、列表和预览示例，明确 slash JSON、英文前缀及 allowlist 帮助布局的展示兼容性变化；逐项对照实际文本契约与 allowlist 文案，确认没有把规划功能写成当前能力，也没有要求程序消费者解析中文。

## 3. 新增只读运行状态

- [ ] 3.1 为当前实例及事件流补充本地状态观察和本次运行计时，保持原连接、重连、断开与任务回收行为；用 fake transport 和可控时钟验证首次连接中、已连接、失败退避/重连、停止、重新启动与墙钟变化，确认 client/任务存在不被当作已连接。
- [ ] 3.2 接入 /milky status 的只读摘要与可信实例归属，支持缺失字段未知、无实例/多实例/归属不明/过期代次整条暂不可用；验证生成状态不发网络请求、不访问图库、不新建后台任务，不从环境或最近会话猜选 profile，时长按规定舍去秒且内部重连不重置。
- [ ] 3.3 接入当前运行规则的字面计数和可信 profile 最新配置的只读比较；用 fake 配置存储验证通配符计数、相同条数但不同集合、空运行集合、Web 保存未应用、非法/不可读配置、规则版本变化与生命周期竞争，区分可信实例仅配置比较未知和实例归属失效整条暂不可用，断言不保存/发布、不泄漏具体名单或敏感配置、不把未知显示为一致或零。
- [ ] 3.4 更新 README 的 status 用法、正常/重连/不同/未知示例与“本次运行”的定义，并在 ARCHITECTURE.md 记录状态所有权及无主动探测边界；对照命令文本契约验证示例一致，运行 slash、生命周期、事件流和 allowlist 聚焦测试通过。

## 4. 验证跨入口与交付边界

- [ ] 4.1 使用 fake host 验证新增帮助、status 和 sticker 别名仍走普通 Gate，allowlist 直接管理例外与 core 独占授权保持原样；同时回归注册无副作用、断开资源释放、命令不进入 Will/buffer/Agent 及不查询管理来源群状态，记录 fixture/fake host 证据。
- [ ] 4.2 回归长帮助、有界贴纸列表和完整 allowlist 列表的分块/合并转发，以及回执发送失败不重复业务操作；以合成受限元数据和异常哨兵验证输出不包含凭证、路径、URL、原始响应或异常正文。
- [ ] 4.3 运行 uv run pytest -q、uv run ruff check .、uv run ruff format --check .、uv build、git diff --check 和 openspec validate --changes --strict；将各命令结果、skip、范围与外部阻塞记入本 change 的 evidence.md，失败不得标为通过。
- [ ] 4.4 记录真实 Hermes/Milky/QQ 验收矩阵：无参数查询、status 正常/重连/配置差异/未知状态、三层帮助、格式错误、列表、预览、单项及部分批次结果、长消息阅读效果；仅在获得真实收发与维护授权的测试环境执行对应项，未执行项明确记为 blocked/unknown，禁止用 fake host 通过替代真实集成结论。
