{
  "name": "milky-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "node build.mjs --check",
    "test": "node --test src/*.test.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.10"
  }
}
